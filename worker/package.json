{
  "name": "melina-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API para Melina Diaz Fotografía",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute melina-db --local --file=./migrations/20260501_add_sobre_mi_table.sql"
  },
  "devDependencies": {
    "wrangler": "^3.77.0"
  },
  "dependencies": {
  }
}
